/**
 * @deprecated Usar controllers/ por rol (Auth, Paciente, Especialista, Secretario, Admin).
 * Se mantiene solo como referencia de migración; las rutas activas están en app/index.js.
 */
module.exports = require('../controllers/AuthController');
